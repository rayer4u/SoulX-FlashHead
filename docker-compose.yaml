services:
  # nginx:
  #   image: nginx:stable
  #   ports:
  #     - "8003:8001"
  #   volumes:
  #     - ./conf/nginx.conf:/etc/nginx/nginx.conf
  #     - ./conf/nginx:/etc/nginx/conf.d
  #   depends_on:
  #     - whisperx
  #   restart: unless-stopped

  server:
    build:
      context: .
      dockerfile: ./Dockerfile
    # entrypoint: bash
    # stdin_open: true
    # tty: true
    ports:
      - 7860:7860
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
    environment:
      - LOG_LEVEL=INFO
      - THREAD_POOL_MAX_WORKERS=2
    restart: unless-stopped
    volumes:
      - ./models:/workspace/models
    # healthcheck:
    #   test: "curl -fsL http://localhost:8000/health > /dev/null"
    #   interval: 10s
    #   timeout: 5s
    #   retries: 3
    #   start_period: 90s